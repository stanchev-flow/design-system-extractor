# On-brand report - hero (HubSpot)

**OVERALL: PASS**

Generated by `brand_pipeline/onbrand_check.py` (brand-agnostic, data-driven) against `replica/index.html`, driven by `brand.yaml`.

## Method

- **neverDo:** every `neverDo[]` rule in `brand.yaml` is dispatched through a brand-agnostic checker registry (supports both hard-edged editorial id families and rounded-SaaS id families) and statically verified against the rendered HTML/CSS. Unknown ids fall back to a keyword heuristic, else report as informational rather than a false fail.
- **Fidelity:** structured checklist derived from the rendered layout's resolved surface, tokens and componentMapping (no per-brand literals). Each row asserts a token/layout fact from the source crop is actually present in the render. Visual confirmation was done by screenshotting the render with headless Chromium and comparing it beside the source crop in `assets/`.
- **Slop checklist:** generic-AI-look failure modes (default fonts, off-palette hex, lorem copy, missing assets, rounding off the brand radius scale, missing webfonts).

## 1. neverDo violations - PASS

| rule | result | detail |
|---|---|---|
| `never-heavy-headings` | PASS | informational: not statically checkable - confirmed by manual visual review |
| `never-dark-mid-page` | PASS | informational: not statically checkable - confirmed by manual visual review |
| `never-text-logos` | PASS | informational: not statically checkable - confirmed by manual visual review |

## 2. Fidelity vs source - PASS

| check | result | source fact |
|---|---|---|
| Surface = brand surface/photo-hero (#55453e) | PASS | section renders on the brand surface bg #55453e |
| Display family present (HubSpot Serif) | PASS | source display tier is HubSpot Serif ~5rem |
| Brand accent surfaced (#ff4800) | PASS | brand accent #ff4800 present in the render |
| Brand assets present (not placeholder) | PASS | 47 local asset(s), missing=none |

## 3. Slop checklist - PASS

| check | result | detail |
|---|---|---|
| No default/system display font | PASS | display font=HubSpot Serif |
| No off-palette hex colors | PASS | off-palette=none; brand palette has 24 hexes |
| No lorem/placeholder copy | PASS | real brand copy from the source inventory |
| All brand image assets present | PASS | local images=47; missing=none |
| Rounding matches brand radius scale | PASS | radius vars=['0.5rem', '0.5rem', '0.25rem', '0.5rem', '0.5rem', '1rem', '50%', '0.5rem', '0.5rem', '0.5rem', '0.5rem', 'var(--button-radius)', '8px', '0.25rem', '1rem']; brand scale=['0.25rem', '0.5rem', '1rem', '50%']; off-scale=none |
| No foreign-brand content literals (alt/aria/title) | PASS | foreign brand names in content attributes=none |
| No foreign-brand asset references | PASS | foreign-owned image refs=none |
| Webfonts loaded (no silent system fallback) | PASS | webfont delivered=True (google-link=True, self-hosted @font-face=True) |

## 5. Composition invariants — HARD (gates OVERALL) — PASS

AI-authored `composition.v1` drift checks (single-accent, primitive-only vocabulary, spacing rhythm, `data-composition` well-formedness, slot resolution) plus the static READABILITY checks (`text-contrast`: real text vs its effective background including any decoration layer behind it; `decoration-salience`: ghost-word/watermark layers must stay close to their surface). These catch drift the visual `neverDo` checks can't see. By default they are ADVISORY (a failure reports as `WARN` and never flips OVERALL); pass `--composition` to make them HARD for AI-composed pages. The existing deterministic pages are gated WITHOUT `--composition`, so these rows never regress them.

| invariant | result | detail |
|---|---|---|
| `single-accent` — Single accent element/role (accent used once) | PASS | accent-role elements applied=1 (<=1 expected per brand no-accent-* rule) |
| `primitive-only` — All elements use the c-* SSOT vocabulary (no bespoke markup) | PASS | non-vocabulary classes=none |
| `rhythm` — Section gaps/padding resolve to the brand spacing scale | PASS | off-scale section spacing=none; brand scale=['0.5rem', '0.75rem 1.5rem', '1.25rem', '1.5rem', '1.875rem', '1rem', '2.5rem', '2rem', '4rem', '5.625rem', '5rem', '67.5rem', '6rem', 'min(100vw - 2rem, 67.5rem)'] |
| `data-composition` — data-composition/pattern metadata is well-formed | PASS | attrs present=10; incoherent=none |
| `slot-resolution` — Zero unresolved slots | PASS | unresolved-slot markers=0 |
| `text-contrast` — Real text clears WCAG-ish contrast vs its effective background (display >= 3.0, body >= 4.5) | PASS | 165 text elements measured against their effective background (surface + decoration layer); worst = page-nav/c-button.c-button--navcta('Get a demo') at 3.4 (floor 4.5); skipped=72; 8 element(s) exempt as MEASURED brand pairs (#ff4800 on #ffffff; #ffffff on #ff4800) |
| `decoration-salience` — Decoration layers (ghost/watermark) stay close to their surface (composite-vs-surface ratio <= 1.19) | PASS | no decoration layers (ghost word / watermark) in this render |
| `occlusion` — Occluded headings (type-behind-media / z:back straddle) honor maxOcclusion + endsVisible (G8 contract) | PASS | no occluded headings (type-behind-media / z:back straddle) in this render |
| `band-attribution` — Banded sections declare + scope both band surfaces (seam contract) | PASS | no banded (dual-surface) sections in this render |
| `alignment-resolution` — Every section's alignment is resolved + source-stamped (section|pattern|style); asymmetric anchors carry a counterweight | PASS | active style declares no alignment stance (or no style) — resolution stamps not required |
| `media-registration` — Resolved-centered sections place split media symmetrically (offset spans require a side anchor / counterweight) | PASS | no resolved-centered statement/quote media in this render |
| `interaction-contrast` — Hover/interaction colors re-scope per surface mode and clear contrast vs their OWN surface (incl. cards/panels) | PASS | 57 hover color(s) measured against their own surface; worst = sec-0/c-button.c-button--secondary('Get started free') at 4.54 (floor 4.5) |
| `token-provenance` — All emitted visual values trace to the brand token index (raw literals need a structural allowlist comment) | PASS | all emitted visual values trace to the HubSpot token index; allowlisted: 36 |
| `logo-wall-integrity` — Logo walls carry disk-backed logo images or text captions (never empty frames, broken srcs, or alt-less marks) | PASS | logo-wall sections=1; problems=none |

## How to view
- Open `replica/index.html` in any browser (it loads the brand fonts from Google Fonts and the source crop in `assets/`).
- A captured preview of the render is at `assets/_preview-render.png`; the source crop it was compared against is `assets/_source-*-crop.*`.
- Re-generate: `python3 brand_pipeline/compose_section.py <brand.yaml> hero -o runs/hubspot-v2/brand/compose/replica`.
- Re-run this gate: `python3 brand_pipeline/onbrand_check.py <brand.yaml> runs/hubspot-v2/brand/compose/replica --layout hero`.
