---
pageType: developer
taskIntents: ["docs/help-center landing", "knowledge-base opener"]
variance: high
requireArchetype: true
excludeArchetypes: [hero-layered-fullbleed-scrim, hero-terminal-split]
---
# Developer hero — HubSpot Developers

Docs-hub landing for developers.hubspot.com. Job: route people to answers FAST —
"Build on the customer platform" plus a working search affordance front and center
("Search the docs — APIs, SDKs, CRM cards, Breeze agent tools"), then the popular
entry points as quiet links: CRM API, UI extensions, OAuth, webhooks. Compact working
masthead; the search field is the hero's primary control.

Emit exactly ONE section: the hero (useCase "hero"). Renderer supplies nav + footer.
Voice: technical, plainspoken, no marketing gloss.

(Note for the record: the terminal/code-panel skeleton is excluded for this brand —
HubSpot v2 carries no mono type tier and no code screenshot asset, so a code panel
cannot bind honestly with current facts.)
