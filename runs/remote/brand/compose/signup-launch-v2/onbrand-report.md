# On-brand report - hero (Remote)

**OVERALL: FAIL**

Generated by `brand_pipeline/onbrand_check.py` (brand-agnostic, data-driven) against `signup-launch-v2/index.html`, driven by `brand.yaml`.

## Method

- **neverDo:** every `neverDo[]` rule in `brand.yaml` is dispatched through a brand-agnostic checker registry (supports both hard-edged editorial id families and rounded-SaaS id families) and statically verified against the rendered HTML/CSS. Unknown ids fall back to a keyword heuristic, else report as informational rather than a false fail.
- **Fidelity:** structured checklist derived from the rendered layout's resolved surface, tokens and componentMapping (no per-brand literals). Each row asserts a token/layout fact from the source crop is actually present in the render. Visual confirmation was done by screenshotting the render with headless Chromium and comparing it beside the source crop in `assets/`.
- **Slop checklist:** generic-AI-look failure modes (default fonts, off-palette hex, lorem copy, missing assets, rounding off the brand radius scale, missing webfonts).

## 1. neverDo violations - PASS

| rule | result | detail |
|---|---|---|
| `never-typographic-primary` | PASS | filled primary CTA button present=True; outline secondary=False |
| `never-zero-radius` | PASS | rounded radius vars=['0.75rem', '0.75rem', '0.75rem', '1.25rem', '0.625rem', '2.5rem', '0.25rem', '2.5rem', '2.5rem', '2.5rem', '2.5rem', '0.75rem', 'var(--button-radius)', 'var(--radius-input)', '0.75rem']; zero-radius UI surface=False |
| `never-allcaps-headings` | PASS | informational: not statically checkable - confirmed by manual visual review |

## 2. Fidelity vs source - PASS

| check | result | source fact |
|---|---|---|
| Surface = a brand surface role (creative-mode scope) | PASS | 7 section(s) on brand roles; off-role: none — source-surface identity re-scoped for archetype-instantiated skeletons |
| Display family present (Bossa) | PASS | source display tier is Bossa ~2.875rem |
| Authored display heading present (creative-mode scope) | PASS | source-copy identity re-scoped: the composition's own display copy must render — authored 5, missing: none |
| Brand assets present (not placeholder) | PASS | 13 local asset(s), missing=none |

## 3. Slop checklist - FAIL

| check | result | detail |
|---|---|---|
| No default/system display font | PASS | display font=Bossa |
| No off-palette hex colors | PASS | off-palette=none; brand palette has 23 hexes |
| No lorem/placeholder copy | PASS | real brand copy from the source inventory |
| All brand image assets present | PASS | local images=13; missing=none |
| Rounding matches brand radius scale | FAIL | radius vars=['0.75rem', '0.75rem', '0.75rem', '1.25rem', '0.625rem', '2.5rem', '0.25rem', '2.5rem', '2.5rem', '2.5rem', '0.75rem', 'var(--button-radius)', 'var(--radius-input)', '0.75rem']; brand scale=['0', '0.25rem', '0.625rem', '2.5rem']; off-scale=['0.75rem', '0.75rem', '0.75rem', '1.25rem', '0.75rem', '0.75rem'] |
| No foreign-brand content literals (alt/aria/title) | PASS | foreign brand names in content attributes=none |
| No foreign-brand asset references | PASS | foreign-owned image refs=none |
| Webfonts loaded (no silent system fallback) | PASS | webfont delivered=True (google-link=True, self-hosted @font-face=False) |

## 4. Style definition — `corporate-saas-clean` — PASS (advisory warnings) (advisory, never gates OVERALL)

The active STYLE layer (`styles/corporate-saas-clean.md`) supplies STRUCTURE + DEFAULTS; the brand supplies hues + fonts AND wins on any value it explicitly sets. These are the style's core `Style definition` rules — brand-overridable defaults, NOT absolute non-negotiables. A deviation backed by a documented brand override (e.g. the hero `#sec-0` centered / accent-gold exception) is reported as an INTENTIONAL OVERRIDE (blessed, logged); a deviation with no backing override is an advisory WARN. Style rows never flip OVERALL — only the brand `neverDo` layer is hard.

| style rule | result | detail |
|---|---|---|
| [INVARIANT] Display rides the brand's measured display tier | PASS | display-size=2.875rem -> rems=[2.875] vs brand display-hero tier 2.875rem (style display_source=brand); matches |
| [INVARIANT] One disciplined accent: reserved for CTAs, links, and key icons only; | WARN | shadows=none; borders=['1px solid #003284', '1px solid var(--c-input-border, var(--c-hairline))', 'var(--button-border)', 'var(--button-border)', 'var(--button-border)', 'var(--button-border)', 'var(--button-border)', 'var(--button-border)', 'var(--button-border)']; card containers=False — style default not met; no backing brand override (advisory) |
| [SOFT] Radius matches brand choice | WARN | --radius=0.75rem; nonzero literal radii=['var(--c-button-radius, var(--radius))', 'var(--c-input-radius, var(--radius))', 'var(--chrome-nav-link-hover-radius)', 'var(--radius-panel, var(--radius))']; style radius default=12px — style default not met; no backing brand override (advisory) |
| [INVARIANT] Comfortable, consistent spacing: generous air, even vertical rhythm, | PASS | data-style+flex-start+left-aligned display present=True (centered text-align count=53) |
| [INVARIANT] Single committed accent (no second accent) | PASS | off-palette hues=none; display headline is ink (not accent)=True |
| Container-query units only (0 viewport units) | PASS | viewport-unit matches=none |

## 5. Composition invariants — HARD (gates OVERALL) — FAIL

AI-authored `composition.v1` drift checks (single-accent, primitive-only vocabulary, spacing rhythm, `data-composition` well-formedness, slot resolution) plus the static READABILITY checks (`text-contrast`: real text vs its effective background including any decoration layer behind it; `decoration-salience`: ghost-word/watermark layers must stay close to their surface). These catch drift the visual `neverDo` checks can't see. By default they are ADVISORY (a failure reports as `WARN` and never flips OVERALL); pass `--composition` to make them HARD for AI-composed pages. The existing deterministic pages are gated WITHOUT `--composition`, so these rows never regress them.

| invariant | result | detail |
|---|---|---|
| `single-accent` — Single accent element/role (accent used once) | PASS | accent-role elements applied=0 (<=1 expected per brand no-accent-* rule) |
| `primitive-only` — All elements use the c-* SSOT vocabulary (no bespoke markup) | PASS | non-vocabulary classes=none |
| `rhythm` — Section gaps/padding resolve to the brand spacing scale | PASS | off-scale section spacing=none; brand scale=['0.25rem', '0.625rem', '0.75rem', '0rem 1.5rem', '1.5rem', '1rem', '2rem', '35.6875rem', '3rem', '45rem', '4rem', '5rem', '6.25rem', '76rem', '7rem', 'min(81.2cqw, 97.5rem)'] |
| `data-composition` — data-composition/pattern metadata is well-formed | PASS | attrs present=5; incoherent=none |
| `slot-resolution` — Zero unresolved slots | PASS | unresolved-slot markers=0 |
| `text-contrast` — Real text clears WCAG-ish contrast vs its effective background (display >= 3.0, body >= 4.5) | PASS | 96 text elements measured against their effective background (surface + decoration layer); worst = page-nav/c-button('Book demo') at 4.91 (floor 4.5); skipped=0 |
| `decoration-salience` — Decoration layers (ghost/watermark) stay close to their surface (composite-vs-surface ratio <= 1.19) | PASS | no decoration layers (ghost word / watermark) in this render |
| `occlusion` — Occluded headings (type-behind-media / z:back straddle) honor maxOcclusion + endsVisible (G8 contract) | PASS | no occluded headings (type-behind-media / z:back straddle) in this render |
| `band-attribution` — Banded sections declare + scope both band surfaces (seam contract) | PASS | no banded (dual-surface) sections in this render |
| `alignment-resolution` — Every section's alignment is resolved + source-stamped (section|curation|pattern|brand|style); asymmetric anchors carry a counterweight | PASS | 6/6 sections resolved + source-stamped; asymmetric anchors all carry counterweights |
| `media-registration` — Resolved-centered sections place split media symmetrically (offset spans require a side anchor / counterweight) | PASS | no resolved-centered statement/quote media in this render |
| `interaction-contrast` — Hover/interaction colors re-scope per surface mode and clear contrast vs their OWN surface (incl. cards/panels) | PASS | 66 hover color(s) measured against their own surface; worst = page-nav/c-arrow-link('Products') at 16.12 (floor 4.5) |
| `token-provenance` — All emitted visual values trace to the brand token index (raw literals need a structural allowlist comment) | PASS | all emitted visual values trace to the Remote token index; allowlisted: 7 |
| `logo-wall-integrity` — Logo walls carry disk-backed logo images or text captions (never empty frames, broken srcs, or alt-less marks) | PASS | logo-wall sections=1; problems=none |
| `knob-consumption` — Every declared knob has a consumer (AS-63) | FAIL | hero: knob `secondaryCta` has NO consumer (not in the renderer registry, not a variantKnob of the section's archetype) — declared intent would silently drop |
| `content-redundancy` — No sibling-slot content redundancy (AS-65) | PASS | composition lints clean |
| `media-binding` — Every media slot resolves or declares its gap | PASS | media bindings clean |
| `mark-legality` — Third-party marks in factual proof contexts only (AS-67) | PASS | media bindings clean |
| `slot-role-eligibility` — Icon-family assets never bound as lead/hero media (AS-80) | PASS | media bindings clean |
| `anatomy-tab-controls` — Declared tab/multi-panel sections render their tab rail (AS-81) | PASS | declared device anatomy present |
| `anatomy-stat-items` — Declared stat sections render their stat items (AS-81) | PASS | declared device anatomy present |
| `fact-consumption` — Every captured measured fact is consumed (AS-83) | FAIL | 0 consumed / 1 unconsumed / 0 excluded (target=generation) — unconsumed: responsive.nav.collapse |
| `fact-consumption:responsive.nav.collapse` — responsive.nav.collapse consumed (AS-83) | FAIL | CAPTURED measured fact with NO consumer evidence in the output (consumer: component_render.nav_collapse_css + compose_section._navbar_props; evidence: CSS marker '(fact-gated: responsive.nav.collapse)') |

## How to view
- Open `signup-launch-v2/index.html` in any browser (it loads the brand fonts from Google Fonts and the source crop in `assets/`).
- A captured preview of the render is at `assets/_preview-render.png`; the source crop it was compared against is `assets/_source-*-crop.*`.
- Re-generate: `python3 brand_pipeline/compose_section.py <brand.yaml> hero -o runs/remote/brand/compose/signup-launch-v2`.
- Re-run this gate: `python3 brand_pipeline/onbrand_check.py <brand.yaml> runs/remote/brand/compose/signup-launch-v2 --layout hero`.
