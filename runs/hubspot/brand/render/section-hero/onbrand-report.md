# On-brand report - hero (HubSpot)

**OVERALL: PASS**

Generated by `brand_pipeline/onbrand_check.py` (brand-agnostic, data-driven) against `section-hero/index.html`, driven by `brand.yaml`.

## Method

- **neverDo:** every `neverDo[]` rule in `brand.yaml` is dispatched through a brand-agnostic checker registry (supports both the WoodWave hard-edged ids and the HubSpot rounded-SaaS ids) and statically verified against the rendered HTML/CSS. Unknown ids fall back to a keyword heuristic, else report as informational rather than a false fail.
- **Fidelity:** structured checklist derived from the rendered layout's resolved surface, tokens and componentMapping (no per-brand literals). Each row asserts a token/layout fact from the source crop is actually present in the render. Visual confirmation was done by screenshotting the render with headless Chromium and comparing it beside the source crop in `assets/`.
- **Slop checklist:** generic-AI-look failure modes (default fonts, off-palette hex, lorem copy, missing assets, rounding off the brand radius scale, missing webfonts).

## 1. neverDo violations - PASS

| rule | result | detail |
|---|---|---|
| `never-typographic-primary` | PASS | filled primary CTA button present=True; outline secondary=True |
| `never-zero-radius` | PASS | rounded radius vars=['0.5rem']; zero-radius UI surface=False |
| `never-blur-hero` | PASS | hero background blur filter present=False (value=none; flat dark scrim only expected) |

## 2. Fidelity vs source - PASS

| check | result | source fact |
|---|---|---|
| Surface = brand surface/base (#fcfcfa) | PASS | section renders on the brand surface bg #fcfcfa |
| Brand accent surfaced (#ff4800) | PASS | brand accent #ff4800 present in the render |
| Brand assets present (not placeholder) | PASS | 1 local asset(s), missing=none |

## 3. Slop checklist - PASS

| check | result | detail |
|---|---|---|
| No default/system display font | PASS | display font=HubSpot Serif Page Header Human |
| No off-palette hex colors | PASS | off-palette=none; brand palette has 12 hexes |
| No lorem/placeholder copy | PASS | real brand copy from the source inventory |
| All brand image assets present | PASS | local images=1; missing=none |
| Rounding matches brand radius scale | PASS | radius vars=['0.5rem']; brand scale=['0.25rem', '0.5rem', '0.75rem', '1rem']; off-scale=none |
| Webfonts loaded (no silent system fallback) | PASS | google fonts link present=True |

## How to view
- Open `section-hero/index.html` in any browser (it loads the brand fonts from Google Fonts and the source crop in `assets/`).
- A captured preview of the render is at `assets/_preview-render.png`; the source crop it was compared against is `assets/_source-*-crop.*`.
- Re-generate: `python3 brand_pipeline/render_section.py <brand.yaml> hero -o runs/hubspot/brand/render/section-hero`.
- Re-run this gate: `python3 brand_pipeline/onbrand_check.py <brand.yaml> runs/hubspot/brand/render/section-hero`.
